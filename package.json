{
  "name": "@finos/web-fdc3",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --all run build"
  },
  "packageManager": "yarn@4.0.2",
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
