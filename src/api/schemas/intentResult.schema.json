{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://fdc3.finos.org/schemas/next/intentResult.schema.json",
    "title": "IntentResult",
    "description": "Describes results that an Intent handler may optionally return that should be communicated back to the app that raised the intent, via the IntentResolution.",
    "type": "object",
    "anyOf": [
        {
            "$ref": "https://fdc3.finos.org/schemas/next/context.schema.json"
        },
        {
            "$ref": "https://fdc3.finos.org/schemas/next/channel.schema.json"
        }
    ]
}