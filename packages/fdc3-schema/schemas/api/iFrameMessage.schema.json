{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://fdc3.finos.org/schemas/next/api/iFrameMessage.schema.json",
  "title": "iframe Message",
  "type": "object",
  "description": "A message used to communicate with iframes injected by `getAgent()` for displaying UI elements such as the intent resolver or channel selector. Used for messages sent in either direction.",
  "properties": {
    "type": {
      "title": "iframe Message type",
      "type": "string",
      "enum": [
        "iframeHello",
        "iframeHandshake",
        "iframeRestyle",
        "iframeDrag",
        "iframeResolve",
        "iframeResolveAction",
        "iframeChannels",
        "iframeChannelSelected"
      ],
      "description": "Identifies the type of the message to or from the iframe."
    },
    "payload": {
      "title": "Message payload",
      "type": "object",
      "description": "The message payload",
      "additionalProperties": true
    }
  },
  "required": [
    "type"
  ],
  "additionalProperties": false
}