ignores:
  # Required by the type-predicate code generator through createRequire().
  - message-await
  # Invoked by the `mkdirs` package script.
  - mkdirp
  # Invoked directly by s2tQuicktypeUtil.cjs during schema generation.
  - quicktype
  # No tslib helpers are imported by the emitted code; likely valid to remove.
  - tslib
