const React = require('react');

export default ({ title, image, content }) => {

    return (
        <div>
            <img src={image} />
            <h3>{t}</h3>)
            {content}
        </div>)
}