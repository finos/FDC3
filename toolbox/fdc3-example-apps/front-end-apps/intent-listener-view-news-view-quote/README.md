# Intent listener 1

This app registers listeners for the `ViewNews` and `ViewQuote` intents with `fdc3.instrument` context. Pair it with an intent-raising example to exercise resolution and multi-intent handling.
