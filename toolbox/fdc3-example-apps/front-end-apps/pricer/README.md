# Pricer

This FDC3 Developer Course sample listens for `ViewQuote` and `demo.GetPrices` on `fdc3.instrument` and shows pricing-oriented UI. It pairs well with intent raises and the security POC apps that return valuations.
