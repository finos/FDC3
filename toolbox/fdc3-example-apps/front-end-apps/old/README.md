# Old FDC3 2.0

This sample uses the legacy `window.fdc3` global pattern instead of `getAgent()`, matching older FDC3 2.x Electron or host integrations. Use it when you need to smoke-test hosts that still inject the API on `window`.
