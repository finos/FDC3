# TradingView

A single SPA uses a `mode` query parameter to switch between symbol info, chart, fundamentals, tickers, and market-data views built with TradingView widgets. The App Directory exports multiple application records that map different intents to the same URL with distinct modes.
