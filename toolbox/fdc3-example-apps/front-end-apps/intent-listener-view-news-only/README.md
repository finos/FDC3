# Intent listener 3

This app registers only for `ViewNews` with `fdc3.instrument` context. Use it when you want a deliberately narrow resolver target or to contrast with listeners that handle multiple intents.
