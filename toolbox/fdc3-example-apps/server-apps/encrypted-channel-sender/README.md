# Encrypted channel sender

This app uses `@finos/fdc3-security` `EncryptedBroadcastSupport` to encrypt context before broadcasting on the current user channel. Run it together with the encrypted channel receiver on the same channel to verify encrypt and decrypt round-trips.
