# Security demo IDP

This lightweight demo identity provider implements `CreateIdentityToken` and returns `fdc3.security.encryptedContext` for the security POC apps. Use it locally when you want token issuance without Microsoft Entra configuration.
