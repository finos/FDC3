# Signed broadcast receiver

Incoming user-channel context is verified with `PublicSignatureCheckingHandlerSupport` using JWKS from attached signatures before the UI displays it. It shows how to drop or isolate contexts that fail signature checks.
