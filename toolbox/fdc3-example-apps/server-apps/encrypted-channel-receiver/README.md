# Encrypted channel receiver

This app listens on the user channel with encrypted-context listener support so payloads decrypt only when keys and configuration match the sender. Use it to validate public-channel encryption wiring inside your Desktop Agent.
